<table>
  <tbody>
    <% post.comments.each do |comment| %>
      <tr>
        <td class="col-2 text-center">
          <div>
            <%= link_to user_path(comment.user.id) do %>
              <%= image_tag comment.user.get_profile_image(50,50), class: "mt-o rounded-circle"; %>
            <% end %>
          </div>
          <div>
            <%= comment.user.name %>
          </div>
        </td>
        <td>
          <div><%= comment.created_at.strftime('%Y/%m/%d') %></div>
          <div><%= comment.comment %></div>
        </td>
        <td>
          <% if comment.user == current_user %>
            <%= link_to '削除', comment_path(comment.id), method: :delete, remote:true, class: "btn btn-sm btn-danger" %>
          <% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>